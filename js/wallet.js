// Future wallet integration placeholder
document.addEventListener("DOMContentLoaded", () => {
  const connected = false; // simulate state

  if (!connected) {
    console.log("👜 No wallet connected. XP in observation mode.");
  } else {
    console.log("✅ Wallet detected. Shrine XP activated.");
  }
});
